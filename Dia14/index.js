/*
Lo que acabas de aprender

Función normal:

function duplicar(numero) {
    return numero * 2;
}

Flecha completa:

const duplicar = (numero) => {
    return numero * 2;
};

Flecha corta:

const duplicar = (numero) => numero * 2;

Y no quiero que memorices las tres como cosas diferentes.

Piensa:

Misma función → diferente forma de escribirla.

Ahora voy a comprobar si realmente lo has entendido antes de enseñarte otra variante.


Qué quiero que te quede

Hay dos situaciones diferentes:

Guardar una función:

const doble = (numero) => numero * 2;

Pasar una función directamente a map():

numeros.map((numero) => numero * 2);
*/
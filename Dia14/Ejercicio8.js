/*
Quiero que crees una función llamada:

duplicar

Debe recibir un número y devolverlo multiplicado por 2.

Por ejemplo:

duplicar(5) → 10
duplicar(20) → 40

🧠 Regla: intenta construirla con lo que ya sabes.

No busques todavía cómo se escribe una función flecha. Quiero comprobar primero qué intuición tienes.
*/

// function duplicar(numero) {
//     return numero * 2;
// };
// console.log(duplicar(5));


// let duplicar = (numero)=>{
//     return numero * 2;
// };
// console.log(duplicar(10));

let duplicar = (nummero) => nummero * 2;
console.log(duplicar(8));